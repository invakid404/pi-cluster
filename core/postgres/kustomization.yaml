apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./helmrelease.yaml
  # The `lowrider` role/database for apps/lowrider-nav. Additive: it creates one
  # role and one database and touches nothing that already exists here.
  - ./lowrider-db-secret.yaml
  - ./lowrider-db-init.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./imageautomation.yaml
configMapGenerator:
  - name: vellum-values
    namespace: apps
    files:
      - values.yaml
configurations:
  - kustomizeconfig.yaml
